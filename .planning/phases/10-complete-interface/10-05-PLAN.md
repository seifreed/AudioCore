---
phase: 10-complete-interface
plan: 05
type: execute
wave: 3
depends_on: ["10-04"]
files_modified:
  - src/audiocore/parallel/__init__.py
  - src/audiocore/parallel/segments.py
  - src/audiocore/parallel/files.py
  - src/audiocore/cli/transcribe.py
  - tests/unit/parallel/test_segments.py
  - tests/unit/parallel/test_files.py
  - tests/unit/parallel/test_imports.py
  - tests/unit/cli/test_transcribe.py
autonomous: true
requirements:
  - PARA-01
  - PARA-02
must_haves:
  truths:
    - Pipeline processes VAD segments in parallel when enabled (module ready for future)
    - CLI processes multiple files concurrently with --parallel flag
    - Results maintain chronological order (sorted by start time for segments, input order for files)
    - Individual file failures don't stop other files in batch mode
    - Memory usage is bounded (configurable worker limits)
    - Progress aggregation shows overall batch progress
  artifacts:
    - path: src/audiocore/parallel/__init__.py
      provides: "Parallel processing module"
      exports: ["transcribe_segments_parallel", "transcribe_files_concurrent", "FileResult"]
    - path: src/audiocore/parallel/segments.py
      provides: "Segment-level parallel processing (future)"
      exports: ["transcribe_segments_parallel"]
    - path: src/audiocore/parallel/files.py
      provides: "File-level concurrent processing"
      exports: ["transcribe_files_concurrent", "FileResult"]
    - path: src/audiocore/cli/transcribe.py
      provides: "CLI with --parallel flag"
      contains: "--parallel option"
  key_links:
    - from: "transcribe_files_concurrent(files, options, max_workers)"
      to: "asyncio.gather with semaphore"
      via: "concurrent file processing"
      pattern: "semaphore = asyncio.Semaphore(max_workers)"
    - from: "CLI --parallel flag"
      to: "transcribe_files_concurrent()"
      via: "batch processing mode"
      pattern: "if options.parallel: transcribe_files_concurrent(...)"
---

# Phase 10: Complete Interface
## Plan 05: Parallel Processing for Segments and Files

<objective>
Implement parallel processing for segment transcription and concurrent file batch processing.

Purpose: Improve performance for multiple segments or files through concurrency.
Output: Parallel processing module with segment and file-level concurrency.
</objective>

<execution_context>
@/Users/seifreed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Core Components
@src/audiocore/pipeline/orchestrator.py
@src/audiocore/backends/base.py
@src/audiocore/models/transcription.py
@src/audiocore/models/segment.py

# Types
@src/audiocore/types/backend.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create parallel processing module structure</name>
  <files>src/audiocore/parallel/__init__.py, src/audiocore/parallel/segments.py</files>
  <action>
    Create parallel processing module with segment placeholder:

    1. Create src/audiocore/parallel/__init__.py:
       - Module docstring for parallel processing
       - Re-export functions from submodules

    2. Create src/audiocore/parallel/segments.py:
       - transcribe_segments_parallel() placeholder function
       - TODO: Future implementation when backends support segment boundaries
       - Current note: backends transcribe full audio

    Important: Current backends (OpenAI, Faster-Whisper) transcribe the 
    complete audio file and generate their own segments. Segment-level
    parallel processing is module structure ready for future enhancement.
    The actual parallel work in v1.0 is at the file level (Task 2).
  </action>
  <verify>
    <automated>python -c "from audiocore.parallel.segments import transcribe_segments_parallel; print('Module loaded')"</automated>
  </verify>
  <done>
    Parallel processing module created:
    - audiocore.parallel module structure ready
    - segments.py module created with placeholder
    - Import test passing
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement concurrent file processing</name>
  <files>src/audiocore/parallel/files.py, tests/unit/parallel/test_files.py</files>
  <action>
    Implement concurrent file transcription:

    1. Create src/audiocore/parallel/files.py:
       - FileResult dataclass: path, result, error
       - transcribe_files_concurrent() async function
       - Use asyncio.Semaphore to limit concurrency
       - asyncio.gather for concurrent execution
       - continue_on_error parameter

    2. Create comprehensive tests in tests/unit/parallel/test_files.py:
       - Test single file transcription
       - Test multiple files concurrent transcription
       - Test max_workers semaphore limiting
       - Test continue_on_error behavior
       - Test progress callback
       - Test result order matches input order
       - Use pytest-asyncio
       - Mock async_transcribe for unit tests

    Note: Use asyncio.Semaphore to limit concurrency.
    Each file runs in its own async task, limited by semaphore.
  </action>
  <verify>
    <automated>pytest tests/unit/parallel/test_files.py -xvs</automated>
  </verify>
  <done>
    Concurrent file processing working:
    - transcribe_files_concurrent() processes multiple files
    - Semaphore limits max concurrent workers
    - Results maintain input order
    - Individual failures don't stop batch when continue_on_error=True
    - All tests passing with >95% coverage
  </done>
</task>

<task type="auto">
  <name>Task 3: Add --parallel flag to CLI and update exports</name>
  <files>src/audiocore/cli/transcribe.py, tests/unit/cli/test_transcribe.py, src/audiocore/parallel/__init__.py, tests/unit/parallel/test_imports.py</files>
  <action>
    Add batch processing support to CLI:

    1. Update src/audiocore/cli/transcribe.py:
       - Update transcribe command signature:
         - input_files: list[Path] (accept multiple files)
         - --parallel, -p: int | None
       - Detect batch mode when multiple input files or --parallel flag
       - Batch mode: use asyncio.run(transcribe_files_concurrent(...))
       - Show progress as [completed/total]
       - Continue on error (show errors for failed files)
       - Exit code 0 if all succeed, 1 if any fail

    2. Update src/audiocore/parallel/__init__.py:
       - Export transcribe_files_concurrent, FileResult, transcribe_segments_parallel

    3. Create tests in tests/unit/parallel/test_imports.py:
       - Test all exports accessible

    4. Add tests in tests/unit/cli/test_transcribe.py:
       - Test single file mode (no --parallel)
       - Test multiple files with --parallel
       - Test --parallel flag limits concurrency
       - Test error handling in batch mode
       - Test exit codes

    The --parallel flag enables concurrent processing:
    - audiocore transcribe file1.mp3 file2.mp3 file3.mp3
    - audiocore transcribe *.mp3 --parallel 2
  </action>
  <verify>
    <automated>pytest tests/unit/cli/test_transcribe.py tests/unit/parallel/test_imports.py -xvs -k "parallel"</automated>
  </verify>
  <done>
    CLI batch mode working:
    - Multiple files as arguments enables batch mode
    - --parallel flag sets max concurrent workers
    - Progress shown as [completed/total]
    - Failures reported but don't stop batch
    - Exit code 1 if any failures
    - All tests passing
  </done>
</task>

</tasks>

<verification>
- [ ] transcribe_files_concurrent() processes multiple files concurrently
- [ ] Semaphore limits max concurrent workers
- [ ] Results maintain input order
- [ ] Individual failures don't stop batch (continue_on_error=True)
- [ ] CLI --parallel flag sets concurrency level
- [ ] Batch mode works with multiple input files
- [ ] Progress aggregation shows completed/total
- [ ] Exit codes meaningful (0 success, 1 any failure)
- [ ] Memory bounded by max_workers limit
</verification>

<success_criteria>
1. Pipeline processes VAD segments in parallel when enabled (module structure ready for future)
2. CLI processes multiple files concurrently with --parallel flag
3. Results maintain chronological order (sorted by input order for files)
4. Individual file failures don't stop other files in batch mode
5. Memory usage is bounded (configurable worker limits)
6. Progress aggregation shows overall batch progress
</success_criteria>

<output>
After completion, create `.planning/phases/10-complete-interface/10-05-SUMMARY.md`
</output>