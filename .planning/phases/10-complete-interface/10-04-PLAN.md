---
phase: 10-complete-interface
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/audiocore/__init__.py
  - src/audiocore/api/__init__.py
  - src/audiocore/api/transcribe.py
  - src/audiocore/errors/__init__.py
  - tests/unit/api/test_transcribe.py
  - tests/unit/api/test_imports.py
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
must_haves:
  truths:
    - Developer can import audiocore and call transcribe(path, options) from Python
    - All library errors are typed exceptions inheriting from AudioCoreError
    - Error messages are actionable and clear
    - Developer can use async_transcribe for non-blocking concurrent transcription
    - API works without CLI installed (library-only usage)
  artifacts:
    - path: src/audiocore/api/__init__.py
      provides: "Public API module"
      exports: ["transcribe", "async_transcribe"]
    - path: src/audiocore/api/transcribe.py
      provides: "Public transcribe functions"
      exports: ["transcribe", "async_transcribe"]
    - path: src/audiocore/__init__.py
      provides: "Main package exports"
      exports: ["transcribe", "async_transcribe", "AudioCoreError", "TranscriptionResult", "TranscriptionOptions"]
  key_links:
    - from: "audiocore.transcribe(path, options)"
      to: "Pipeline.transcribe()"
      via: "synchronous convenience function"
      pattern: "pipeline = Pipeline(); return pipeline.transcribe(...)"
    - from: "audiocore.async_transcribe(path, options)"
      to: "asyncio.to_thread(Pipeline.transcribe)"
      via: "thread pool executor"
      pattern: "await asyncio.to_thread(transcribe, ...)"
---

# Phase 10: Complete Interface
## Plan 04: Public API with Sync and Async Support

<objective>
Create a clean public Python API for programmatic use with synchronous and asynchronous transcription.

Purpose: Enable developers to use AudioCore as a library without CLI, with async support for concurrent transcription.
Output: Public API module with transcribe() and async_transcribe() functions.
</objective>

<execution_context>
@/Users/seifreed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Core Components
@src/audiocore/pipeline/orchestrator.py
@src/audiocore/models/transcription.py
@src/audiocore/errors/__init__.py

# Existing Public API
@src/audiocore/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API module structure and verify error hierarchy</name>
  <files>src/audiocore/api/__init__.py, src/audiocore/api/transcribe.py, src/audiocore/errors/__init__.py, tests/unit/api/test_imports.py</files>
  <action>
    Create dedicated API module and verify error hierarchy:

    1. Create src/audiocore/api/__init__.py:
       - Module docstring explaining public API
       - Re-export transcribe and async_transcribe
       - Re-export TranscriptionResult, TranscriptionOptions
       - Re-export AudioCoreError and common exceptions

    2. Create src/audiocore/api/transcribe.py:
       - Import Pipeline from audiocore.pipeline
       - Note: sync transcribe() exists in audiocore/__init__.py (we'll link to it)

    3. Verify src/audiocore/errors/__init__.py exports:
       - All exceptions accessible
       - All inherit from AudioCoreError
       - All have unique error codes

    4. Create test in tests/unit/api/test_imports.py:
       - Test all exceptions importable
       - Test all inherit from AudioCoreError
       - Test error codes are unique

    Note: The sync transcribe() already exists in audiocore/__init__.py.
    For organizational clarity, we'll also expose it from audiocore.api.
  </action>
  <verify>
    <automated>pytest tests/unit/api/test_imports.py -xvs -k "test_error_hierarchy"</automated>
  </verify>
  <done>
    API module structure created:
    - audiocore.api module accessible
    - Error hierarchy complete and accessible
    - All errors inherit from AudioCoreError
    - Import tests passing
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement async_transcribe function</name>
  <files>src/audiocore/api/transcribe.py, tests/unit/api/test_transcribe.py</files>
  <action>
    Implement async version using thread pool:

    1. Add async_transcribe() to api/transcribe.py:
       - Use asyncio.run_in_executor() with thread pool
       - ThreadPoolExecutor with max_workers=4
       - Accept same parameters as sync transcribe()
       - Return TranscriptionResult via await

    2. Create comprehensive tests in tests/unit/api/test_transcribe.py:
       - Test async_transcribe returns TranscriptionResult
       - Test concurrent transcription with asyncio.gather
       - Test error propagation
       - Use pytest-asyncio for async tests
       - Mock Pipeline.transcribe for unit tests

    Note: asyncio.run_in_executor() runs sync code in background thread.
    Progress callbacks work but called from background thread.
  </action>
  <verify>
    <automated>pytest tests/unit/api/test_transcribe.py -xvs</automated>
  </verify>
  <done>
    async_transcribe() working:
    - Returns TranscriptionResult via await
    - Runs in thread pool (non-blocking event loop)
    - Concurrent transcription works with asyncio.gather
    - All tests passing
  </done>
</task>

<task type="auto">
  <name>Task 3: Update main package exports</name>
  <files>src/audiocore/__init__.py, tests/unit/api/test_imports.py</files>
  <action>
    Update main package to expose public API:

    1. Update src/audiocore/__init__.py:
       - Keep existing transcribe import from pipeline
       - Add async_transcribe from api.transcribe
       - Add common exception imports
       - Add TranscriptionResult, TranscriptionOptions
       - Add BackendType, OutputFormat, SelectionPolicy
       - Comprehensive __all__ list

    2. Add tests in tests/unit/api/test_imports.py:
       - Test all __all__ exports accessible
       - Test common usage pattern
       - Test exception catching
  </action>
  <verify>
    <automated>pytest tests/unit/api/test_imports.py -xvs</automated>
  </verify>
  <done>
    Main package exports complete:
    - All public symbols in __all__
    - Clean import: from audiocore import transcribe, TranscriptionResult
    - Exception hierarchy fully accessible
    - All tests passing
  </done>
</task>

</tasks>

<verification>
- [ ] transcribe(path, options) accessible from audiocore
- [ ] async_transcribe(path, options) accessible from audiocore
- [ ] All exceptions inherit from AudioCoreError
- [ ] All error codes are unique and follow category pattern
- [ ] Library use works without CLI installed
- [ ] Concurrent transcription works with asyncio.gather
- [ ] All imports in __all__ test passing
</verification>

<success_criteria>
1. Developer can import audiocore and call transcribe(path, options) from Python
2. All library errors are typed exceptions inheriting from AudioCoreError
3. Error messages are actionable and clear
4. Developer can use async_transcribe for non-blocking concurrent transcription
5. API works without CLI installed (library-only usage)
</success_criteria>

<output>
After completion, create `.planning/phases/10-complete-interface/10-04-SUMMARY.md`
</output>