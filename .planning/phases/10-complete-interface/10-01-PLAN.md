---
phase: 10-complete-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audiocore/output/srt.py
  - src/audiocore/output/vtt.py
  - src/audiocore/output/__init__.py
  - tests/unit/output/test_srt.py
  - tests/unit/output/test_vtt.py
autonomous: true
requirements:
  - OUT-03
  - OUT-04
must_haves:
  truths:
    - User can export transcriptions as SRT subtitles compatible with video players
    - User can export transcriptions as VTT format for web players
    - SRT format has sequential numbering and proper timestamp format
    - VTT format has WEBVTT header and proper cue format
  artifacts:
    - path: src/audiocore/output/srt.py
      provides: "SRT formatter function"
      exports: ["format_srt"]
    - path: src/audiocore/output/vtt.py
      provides: "VTT formatter function"
      exports: ["format_vtt"]
    - path: src/audiocore/output/__init__.py
      provides: "Module exports"
      contains: "format_srt, format_vtt"
  key_links:
    - from: "format_srt(result, options)"
      to: "TranscriptionResult.segments"
      via: "iteration over segments"
      pattern: "for i, segment in enumerate(result.segments)"
    - from: "format_vtt(result, options)"
      to: "TranscriptionResult.segments"
      via: "iteration over segments"
      pattern: "WEBVTT header + cue blocks"
---

# Phase 10: Complete Interface
## Plan 01: SRT and VTT Output Serializers

<objective>
Implement SRT and VTT subtitle format serializers for transcription results.

Purpose: Enable users to export transcriptions as subtitles compatible with video players and web contexts.
Output: format_srt() and format_vtt() functions in src/audiocore/output/ module.
</objective>

<execution_context>
@/Users/seifreed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior Work (Phase 9)
@.planning/phases/09-pipeline-orchestrator/09-03-SUMMARY.md

# Existing Format Patterns
@src/audiocore/output/text.py
@src/audiocore/output/json.py

# Type Definitions
@src/audiocore/types/format.py
@src/audiocore/models/transcription.py
@src/audiocore/models/segment.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SRT formatter</name>
  <files>src/audiocore/output/srt.py, tests/unit/output/test_srt.py</files>
  <action>
    Create format_srt() function following the pattern from format_text():

    1. Create src/audiocore/output/srt.py with:
       - _format_srt_timestamp(seconds: float) -> str helper
         Format: HH:MM:SS,mmm (comma for milliseconds, SRT standard)
       - format_srt(result: TranscriptionResult, options: TranscriptionOptions) -> str
         Output format:
         1
         00:00:00,000 --> 00:00:05,234
         Hello world

         2
         00:00:05,500 --> 00:00:10,000
         How are you?

    2. Handle empty segments list (return empty string)
    3. Handle empty text gracefully (output cue with empty text)
    4. Use 1-based sequential numbering
    5. Create comprehensive tests in tests/unit/output/test_srt.py:
       - Test timestamp formatting (edge cases: 0, hours rollover, milliseconds)
       - Test sequential numbering
       - Test empty segments
       - Test empty text in segments
       - Test special characters (newlines, unicode)
       - Test multiline text in segments
       - Use mock TranscriptionResult with Segment objects

    Reference format_text() in text.py for the pure function pattern.
    SRT spec: each cue has index, timestamp line, text line(s), blank line separator.
  </action>
  <verify>
    <automated>pytest tests/unit/output/test_srt.py -xvs</automated>
  </verify>
  <done>
    format_srt() produces valid SRT output with:
    - Sequential numbering starting from 1
    - Timestamps in HH:MM:SS,mmm format (comma for milliseconds)
    - All tests passing with >95% coverage
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement VTT formatter</name>
  <files>src/audiocore/output/vtt.py, tests/unit/output/test_vtt.py</files>
  <action>
    Create format_vtt() function following the pattern from format_text():

    1. Create src/audiocore/output/vtt.py with:
       - _format_vtt_timestamp(seconds: float) -> str helper
         Format: HH:MM:SS.mmm (dot for milliseconds, VTT standard)
       - format_vtt(result: TranscriptionResult, options: TranscriptionOptions) -> str
         Output format:
         WEBVTT

         00:00:00.000 --> 00:00:05.234
         Hello world

         00:00:05.500 --> 00:00:10.000
         How are you?

    2. Include WEBVTT header line at start
    3. Handle empty segments list (return WEBVTT header only)
    4. Handle empty text gracefully
    5. No sequential numbering in VTT (unlike SRT)
    6. Create comprehensive tests in tests/unit/output/test_vtt.py:
       - Test timestamp formatting (dot separator for milliseconds)
       - Test WEBVTT header presence
       - Test empty segments (WEBVTT header only)
       - Test empty text in segments
       - Test special characters
       - Test multiline text
       - Use mock TranscriptionResult with Segment objects

    Reference format_text() and format_srt() for pattern consistency.
    VTT spec: WEBVTT header, double newline, timestamp cue pairs with dot separator.
  </action>
  <verify>
    <automated>pytest tests/unit/output/test_vtt.py -xvs</automated>
  </verify>
  <done>
    format_vtt() produces valid VTT output with:
    - WEBVTT header
    - Timestamps in HH:MM:SS.mmm format (dot for milliseconds)
    - No sequential numbering
    - All tests passing with >95% coverage
  </done>
</task>

<task type="auto">
  <name>Task 3: Update output module exports</name>
  <files>src/audiocore/output/__init__.py, tests/unit/output/test_imports.py</files>
  <action>
    Update src/audiocore/output/__init__.py to export the new formatters:

    1. Add format_srt and format_vtt to imports and __all__
    2. Update module docstring to mention SRT and VTT
    3. Create test to verify all formatters exported correctly:
       - from audiocore.output import format_text, format_json, format_srt, format_vtt
       - Verify all are callable functions
  </action>
  <verify>
    <automated>pytest tests/unit/output/test_imports.py -xvs</automated>
  </verify>
  <done>
    All output formatters exported correctly:
    - format_text, format_json, format_srt, format_vtt available from audiocore.output
    - Import test passing
  </done>
</task>

</tasks>

<verification>
- [ ] format_srt() produces valid SRT with sequential numbering and comma timestamps
- [ ] format_vtt() produces valid VTT with WEBVTT header and dot timestamps
- [ ] Both formatters handle edge cases (empty segments, special chars, multiline)
- [ ] All formatters exported from audiocore.output module
- [ ] >95% test coverage on srt.py and vtt.py
</verification>

<success_criteria>
1. User can export transcriptions as SRT subtitles compatible with video players
2. User can export transcriptions as VTT format for web players
3. SRT format has sequential numbering starting from 1
4. SRT timestamps use comma for milliseconds (HH:MM:SS,mmm)
5. VTT format has WEBVTT header
6. VTT timestamps use dot for milliseconds (HH:MM:SS.mmm)
7. Both formatters handle empty segments and special characters correctly
</success_criteria>

<output>
After completion, create `.planning/phases/10-complete-interface/10-01-SUMMARY.md`
</output>