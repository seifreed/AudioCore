---
phase: 10-complete-interface
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - src/audiocore/output/file_writer.py
  - tests/unit/output/test_file_writer.py
  - src/audiocore/errors/output.py
  - src/audiocore/errors/__init__.py
  - tests/unit/output/test_imports.py
autonomous: true
requirements:
  - OUT-05
must_haves:
  truths:
    - User can write output to file with automatic directory creation
    - User gets overwrite protection when file exists (configurable)
    - Files are written atomically (temp file then move)
    - Stdout works when no output file specified
  artifacts:
    - path: src/audiocore/output/file_writer.py
      provides: "File writing utilities"
      exports: ["write_output", "OutputFileConfig", "format_and_write"]
    - path: src/audiocore/errors/output.py
      provides: "Output file errors"
      exports: ["OutputFileExistsError"]
    - path: tests/unit/output/test_file_writer.py
      provides: "Test coverage for file writing"
  key_links:
    - from: "write_output(content, path, config)"
      to: "format_srt/format_vtt formatters"
      via: "format_result function"
      pattern: "formatters.format(result)"
    - from: "OutputFileConfig.overwrite"
      to: "file existence check"
      via: "path.exists() check"
      pattern: "if path.exists() and not overwrite: raise"
---

# Phase 10: Complete Interface
## Plan 02: File Output with Directory Creation

<objective>
Implement file output writer with directory creation and overwrite protection.

Purpose: Enable users to write formatted transcriptions to files with robust handling of paths.
Output: write_output() function in src/audiocore/output/file_writer.py with OutputFileConfig.
</objective>

<execution_context>
@/Users/seifreed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Existing Patterns
@src/audiocore/output/text.py
@src/audiocore/output/json.py

# Error Handling Pattern
@src/audiocore/errors/base.py

# Types
@src/audiocore/types/format.py
@src/audiocore/models/transcription.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OutputFileConfig model and write_output function</name>
  <files>src/audiocore/output/file_writer.py, src/audiocore/errors/output.py, tests/unit/output/test_file_writer.py</files>
  <action>
    Create file output utilities with atomic writing:

    1. Create src/audiocore/errors/output.py:
       - OutputFileExistsError inheriting from AudioCoreError
       - Error code AUD-600 (new output errors category)
       - Context includes file path
       - Suggestion: "Set overwrite=True to replace existing file"

    2. Create src/audiocore/output/file_writer.py:
       - OutputFileConfig Pydantic model:
         - overwrite: bool = False
         - create_dirs: bool = True
         - encoding: str = "utf-8"
       - Use strict=True, extra="forbid"

       - write_output(content: str, path: Path | str, config: OutputFileConfig | None = None) -> Path:
         - Convert path to Path object
         - Check file existence if overwrite=False
         - Create parent directories if create_dirs=True
         - Write to temp file, then move (atomic)
         - Return path to written file

    3. Create tests in tests/unit/output/test_file_writer.py:
       - Test directory creation
       - Test overwrite protection
       - Test atomic write
       - Test encoding handling
       - Use tempfile for isolation

    Reference temp_audio_file context manager pattern from media extractor.
  </action>
  <verify>
    <automated>pytest tests/unit/output/test_file_writer.py -xvs</automated>
  </verify>
  <done>
    OutputFileConfig and write_output working:
    - Creates parent directories automatically
    - Raises OutputFileExistsError when file exists and overwrite=False
    - Atomic write (temp then move)
    - All tests passing
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement format_and_write helper and update exports</name>
  <files>src/audiocore/output/file_writer.py, src/audiocore/errors/__init__.py, tests/unit/output/test_file_writer.py, tests/unit/output/test_imports.py</files>
  <action>
    Add format detection and update module exports:

    1. Add format_and_write() to file_writer.py:
       - Auto-detect format from file extension
       - Use OutputFormat.parse() for extension parsing
       - Call appropriate formatter
       - Write using write_output()

    2. Update src/audiocore/errors/__init__.py:
       - Export OutputFileExistsError

    3. Update src/audiocore/output/__init__.py:
       - Export write_output, OutputFileConfig, format_and_write

    4. Add tests for format_and_write:
       - Test format detection from extension
       - Test fallback to options.output_format
       - Test end-to-end: result -> formatted file
       - Update import tests

    One-line API: format_and_write(result, options, "output.srt")
  </action>
  <verify>
    <automated>pytest tests/unit/output/test_file_writer.py tests/unit/output/test_imports.py -xvs</automated>
  </verify>
  <done>
    File writing complete:
    - format_and_write() auto-detects format from extension
    - All exports available from audiocore.output
    - OutputFileExistsError available from audiocore.errors
    - All tests passing
  </done>
</task>

</tasks>

<verification>
- [ ] OutputFileConfig with overwrite, create_dirs, encoding fields
- [ ] write_output() creates directories atomically
- [ ] OutputFileExistsError raised for existing files (when overwrite=False)
- [ ] Atomic write (temp file then move)
- [ ] format_and_write() auto-detects format from extension
- [ ] All formatters integrated (text, json, srt, vtt)
- [ ] All tests passing with >95% coverage
</verification>

<success_criteria>
1. User can write output to file with directory creation
2. Overwrite protection prevents accidental data loss
3. Files written atomically (temp then move)
4. Format auto-detected from file extension
5. Clear error message when file exists and overwrite=False
</success_criteria>

<output>
After completion, create `.planning/phases/10-complete-interface/10-02-SUMMARY.md`
</output>