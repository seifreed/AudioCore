---
phase: 10-complete-interface
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/audiocore/cli/__init__.py
  - src/audiocore/cli/main.py
  - src/audiocore/cli/transcribe.py
  - src/audiocore/cli/backends.py
  - src/audiocore/cli/models.py
  - src/audiocore/cli/config_cmd.py
  - pyproject.toml
  - tests/unit/cli/test_transcribe.py
  - tests/unit/cli/test_backends.py
  - tests/unit/cli/test_models.py
  - tests/unit/cli/test_config_cmd.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
must_haves:
  truths:
    - User can run 'audiocore transcribe <file>' with all configuration options
    - User can run 'audiocore backends list' and 'audiocore backends check' commands
    - User can run 'audiocore models list', 'download', 'remove' for faster-whisper models
    - User can run 'audiocore config show' with API keys redacted
    - All commands have comprehensive --help text
    - Exit codes are meaningful (0 success, non-zero error)
  artifacts:
    - path: src/audiocore/cli/main.py
      provides: "Main CLI entry point"
      exports: ["app"]
    - path: src/audiocore/cli/transcribe.py
      provides: "Transcribe command"
      exports: ["transcribe"]
    - path: src/audiocore/cli/backends.py
      provides: "Backend management commands"
      exports: ["backends_app", "list_backends", "check_backends"]
    - path: src/audiocore/cli/models.py
      provides: "Model management commands"
      exports: ["models_app", "list_models", "download_model", "remove_model"]
    - path: src/audiocore/cli/config_cmd.py
      provides: "Config display commands"
      exports: ["config_app", "show_config", "config_path"]
    - path: pyproject.toml
      provides: "CLI entry point"
      contains: "[project.scripts]"
  key_links:
    - from: "audiocore transcribe"
      to: "audiocore.pipeline.transcribe"
      via: "calling transcribe() function"
      pattern: "from audiocore import transcribe"
    - from: "audiocore config show"
      to: "audiocore.config.load_config"
      via: "loading and redacting configuration"
      pattern: "mask_secrets(app_config)"
---

# Phase 10: Complete Interface
## Plan 03: CLI Commands Implementation

<objective>
Implement comprehensive CLI using Typer with all transcription and management commands.

Purpose: Provide intuitive command-line interface for all AudioCore functionality.
Output: CLI module with transcribe, backends, models, and config commands.
</objective>

<execution_context>
@/Users/seifreed/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/seifreed/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior Work
@.planning/phases/09-pipeline-orchestrator/09-01-SUMMARY.md

# Core Components
@src/audiocore/pipeline/orchestrator.py
@src/audiocore/config/settings.py
@src/audiocore/backends/registry.py
@src/audiocore/backends/availability.py

# Types
@src/audiocore/types/backend.py
@src/audiocore/types/format.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLI module structure with transcribe command</name>
  <files>src/audiocore/cli/__init__.py, src/audiocore/cli/main.py, src/audiocore/cli/transcribe.py, tests/unit/cli/test_transcribe.py</files>
  <action>
    Create CLI module using Typer framework:

    1. Create src/audiocore/cli/__init__.py - export app
    2. Create src/audiocore/cli/main.py - Typer app with rich progress display
    3. Create src/audiocore/cli/transcribe.py:
       - Arguments: input_file (required), --output/-o, --format/-f, --backend/-b, --language/-l, --model/-m, --output-dir/-d
       - Progress display using rich.progress
       - Call Pipeline().transcribe()
       - Handle exceptions and convert to exit codes
       - Print to stdout if no output file

    4. Create tests in tests/unit/cli/test_transcribe.py:
       - Test command invocation with various options
       - Test output to file and stdout
       - Test error handling and exit codes
       - Use CliRunner from typer.testing

    Add typer and rich dependencies if not present in pyproject.toml.
  </action>
  <verify>
    <automated>pytest tests/unit/cli/test_transcribe.py -xvs</automated>
  </verify>
  <done>
    Transcribe command working:
    - 'audiocore transcribe <file>' works
    - All configuration options accessible via flags
    - Progress display during transcription
    - Output to file or stdout based on options
    - Exit codes: 0 success, 1-4 errors
    - All tests passing
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement backend management commands</name>
  <files>src/audiocore/cli/backends.py, tests/unit/cli/test_backends.py</files>
  <action>
    Create backend list and check commands:

    1. Create src/audiocore/cli/backends.py:
       - backends_app Typer subcommand group
       - list_backends: Lists all backends with status
       - check_backends: Validates configuration, exit 0 if at least one available

    2. Update main.py to register backends_app

    3. Create tests in tests/unit/cli/test_backends.py:
       - Test list_backends output format
       - Test check_backends validation
       - Test exit codes
       - Mock BackendRegistry and is_backend_available

    Use is_backend_available() from audiocore.backends.availability.
  </action>
  <verify>
    <automated>pytest tests/unit/cli/test_backends.py -xvs</automated>
  </verify>
  <done>
    Backend commands working:
    - 'audiocore backends list' shows available backends
    - 'audiocore backends check' validates configuration
    - Clean table output with status indicators
    - All tests passing
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement model management commands</name>
  <files>src/audiocore/cli/models.py, tests/unit/cli/test_models.py</files>
  <action>
    Create model management commands for faster-whisper:

    1. Create src/audiocore/cli/models.py:
       - models_app Typer subcommand group
       - list_models: Show cached models vs available
       - download_model: Download from HuggingFace with progress bar
       - remove_model: Delete cached model with confirmation

    2. Update main.py to register models_app

    3. Create tests in tests/unit/cli/test_models.py:
       - Test list_models with/without cached models
       - Test download_model validation
       - Test remove_model confirmation
       - Mock ModelManager methods

    Reference ModelManager from audiocore.backends.faster_whisper.model_manager.
  </action>
  <verify>
    <automated>pytest tests/unit/cli/test_models.py -xvs</automated>
  </verify>
  <done>
    Model commands working:
    - 'audiocore models list' shows available and cached models
    - 'audiocore models download <model>' downloads from HuggingFace
    - 'audiocore models remove <model>' deletes cached models
    - Progress display during download
    - Confirmation before deletion
    - All tests passing
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement config display command and entry point</name>
  <files>src/audiocore/cli/config_cmd.py, pyproject.toml, tests/unit/cli/test_config_cmd.py</files>
  <action>
    Create config display command and update entry point:

    1. Create src/audiocore/cli/config_cmd.py:
       - config_app Typer subcommand group
       - show_config: Display configuration with API keys redacted (sk-***)
       - config_path: Show default config file location

    2. Update pyproject.toml:
       - Add [project.scripts]: audiocore = "audiocore.cli.main:app"
       - Verify typer and rich dependencies

    3. Create tests in tests/unit/cli/test_config_cmd.py:
       - Test show_config output
       - Test API key redaction
       - Test config_path output

    4. Update main.py to register config_app
  </action>
  <verify>
    <automated>pytest tests/unit/cli/test_config_cmd.py -xvs && python -c "from audiocore.cli.main import app; print('CLI installed')"</automated>
  </verify>
  <done>
    Config commands and entry point working:
    - 'audiocore config show' displays configuration with redacted keys
    - 'audiocore config path' shows config file location
    - 'audiocore' command available after installation
    - All tests passing
  </done>
</task>

</tasks>

<verification>
- [ ] 'audiocore transcribe <file>' works with all options
- [ ] 'audiocore backends list' shows available backends
- [ ] 'audiocore backends check' validates configuration
- [ ] 'audiocore models list' shows available models
- [ ] 'audiocore models download <model>' downloads models
- [ ] 'audiocore models remove <model>' removes models
- [ ] 'audiocore config show' displays redacted configuration
- [ ] 'audiocore config path' shows config location
- [ ] All commands have --help text
- [ ] Meaningful exit codes
- [ ] All tests passing with >95% coverage
</verification>

<success_criteria>
1. User can run 'audiocore transcribe <file>' with all configuration options
2. User can run 'audiocore backends list' and 'audiocore backends check' commands
3. User can run 'audiocore models list', 'download', 'remove' for faster-whisper models
4. User can run 'audiocore config show' with API keys redacted
5. All commands have comprehensive --help text
6. Exit codes are meaningful (0 success, non-zero error)
</success_criteria>

<output>
After completion, create `.planning/phases/10-complete-interface/10-03-SUMMARY.md`
</output>